<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Долгих Илья — Визитка</title>
  <meta name="description" content="Долгих Илья — разработчик / предприниматель. Услуги: сайты, боты, автоматизация. Свяжитесь для заказа." />
  <meta property="og:title" content="Долгих Илья — Визитка" />
  <meta property="og:description" content="Разработка сайтов, ботов и автоматизация процессов. Пишите — быстро и качественно." />
  <meta property="og:type" content="website" />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='18' fill='%230f1724'/><text x='50' y='58' font-size='48' text-anchor='middle' fill='%23fff' font-family='Arial'>И</text></svg>">

  <style>
    :root{
      --bg:#0f1724;
      --card:#071018;
      --accent:#22c55e;
      --muted:#94a3b8;
      --glass: rgba(255,255,255,0.03);
      --glass-2: rgba(255,255,255,0.06);
      --radius:14px;
      --maxw:980px;
      --gutter:20px;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; -webkit-font-smoothing:antialiased;}
    body{
      background:
        radial-gradient(900px 400px at 10% 10%, rgba(34,197,94,0.06), transparent 10%),
        radial-gradient(700px 300px at 90% 90%, rgba(34,197,94,0.03), transparent 10%),
        var(--bg);
      color:#e6eef8;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:40px 20px;
    }

    .wrap{
      width:100%;
      max-width:var(--maxw);
      display:grid;
      grid-template-columns: 320px 1fr;
      gap:var(--gutter);
      align-items:start;
    }

    .card{
      background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:var(--radius);
      padding:18px;
      border:1px solid var(--glass-2);
      box-shadow: 0 10px 30px rgba(2,6,12,0.6);
    }

    /* Left column — profile */
    .profile{
      display:flex;
      flex-direction:column;
      gap:12px;
    }
    .avatar{
      width:100%;
      height:240px;
      border-radius:12px;
      overflow:hidden;
      background:linear-gradient(135deg,#0b1220,#071018);
      display:flex;
      align-items:center;
      justify-content:center;
      border:1px solid rgba(255,255,255,0.03);
    }
    .avatar img{width:100%; height:100%; object-fit:cover; display:block;}
    .name {font-size:20px; font-weight:800;}
    .role {color:var(--muted); font-weight:600; font-size:13px;}
    .tags{display:flex; gap:8px; flex-wrap:wrap; margin-top:6px;}
    .tag{background:var(--glass); padding:6px 8px; border-radius:8px; font-weight:700; color:var(--muted); font-size:13px;}

    .contact-list{display:flex; flex-direction:column; gap:8px; margin-top:8px;}
    .contact-item{display:flex; gap:10px; align-items:center; padding:8px; border-radius:10px; background:rgba(255,255,255,0.02);}
    .contact-item a{color:inherit; text-decoration:none; font-weight:700;}
    .contact-item small{display:block; color:var(--muted); font-weight:600; font-size:12px;}

    .btn{
      display:inline-flex; align-items:center; gap:8px; padding:10px 12px; border-radius:10px; font-weight:800; cursor:pointer;
      border:none; background:linear-gradient(90deg,#16a34a,#22c55e); color:#071013;
    }
    .btn.outline{background:transparent; border:1px solid var(--glass-2); color:var(--muted); font-weight:700;}

    /* Right column — content */
    .content{display:flex; flex-direction:column; gap:14px;}
    h1{margin:0; font-size:22px;}
    p.lead{color:var(--muted); margin:6px 0 0 0;}
    .section{background:transparent; padding:12px; border-radius:10px;}
    .services{display:grid; grid-template-columns:repeat(auto-fit,minmax(160px,1fr)); gap:10px;}
    .service{background:var(--glass); padding:10px; border-radius:10px; min-height:72px;}
    .portfolio{display:grid; grid-template-columns:repeat(auto-fit,minmax(180px,1fr)); gap:12px;}
    .port-item{border-radius:10px; overflow:hidden; border:1px solid rgba(255,255,255,0.03); background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent); padding:10px;}
    .port-item img{width:100%; height:120px; object-fit:cover; border-radius:8px; display:block;}

    .contact-form{display:grid; grid-template-columns:1fr 160px; gap:10px; align-items:start;}
    .input, textarea{width:100%; padding:10px; border-radius:8px; border:1px solid rgba(255,255,255,0.04); background:transparent; color:inherit; resize:vertical;}
    textarea{min-height:100px;}
    .actions{display:flex; gap:8px; margin-top:8px; flex-wrap:wrap;}

    .foot{color:var(--muted); font-size:13px; text-align:center; margin-top:6px;}

    /* small screens */
    @media(max-width:880px){
      .wrap{grid-template-columns:1fr; max-width:720px;}
      .avatar{height:200px;}
      .contact-form{grid-template-columns:1fr;}
    }

    /* Light theme toggle (class on body) */
    body.light{
      --bg: #f6fbf6;
      --card: #ffffff;
      --accent: #0b815a;
      --muted: #334155;
      color:#071013;
      background: linear-gradient(180deg,#f6fbf6,#eef6ee);
    }
    body.light .btn{color:#fff}
    body.light .btn.outline{color:var(--muted)}
  </style>
</head>
<body>
  <div class="wrap" role="main" aria-label="Сайт-визитка">
    <!-- LEFT -->
    <div class="card profile" aria-label="Профиль">
      <div class="avatar" id="avatar">
        <!-- Replace src with your photo or use background color -->
        <img src="https://images.unsplash.com/photo-1545996124-1b8d1b9a3c4f?q=80&w=1200&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Фото">
      </div>

      <div>
        <div class="name" id="personName">Долгих Илья</div>
        <div class="role" id="personRole">Разработчик · Автоматизация · Сайты</div>
        <div class="tags" aria-hidden="true">
          <div class="tag">Сайты</div>
          <div class="tag">Боты</div>
          <div class="tag">Автоматизация</div>
        </div>
      </div>

      <div class="contact-list" style="margin-top:6px;">
        <div class="contact-item">
          <div style="font-weight:900">📞</div>
          <div>
            <a href="tel:+79990000000" id="phoneLink">+7 (999) 000-00-00</a>
            <small>Телефон</small>
          </div>
        </div>

        <div class="contact-item">
          <div style="font-weight:900">✉️</div>
          <div>
            <a href="mailto:hello@example.com" id="emailLink">hello@example.com</a>
            <small>Email</small>
          </div>
        </div>

        <div class="contact-item">
          <div style="font-weight:900">📍</div>
          <div>
            <span id="locationText">Красноярск, Россия</span>
            <small>Локация</small>
          </div>
        </div>
      </div>

      <div style="display:flex; gap:8px; margin-top:10px;">
        <button class="btn" id="downloadVcard">Скачать vCard</button>
        <button class="btn outline" id="themeToggle">Светлая тема</button>
      </div>

      <div style="margin-top:12px;">
        <a class="btn outline" href="https://t.me/" id="tgLink" target="_blank" rel="noopener">Telegram</a>
      </div>

      <div class="foot">© <span id="year"></span> Долгих Илья — Быстро. Честно. Без воды.</div>
    </div>

    <!-- RIGHT -->
    <div class="content">
      <div class="card section">
        <h1>Привет — я <span id="hiName">Долгих Илья</span></h1>
        <p class="lead">Делаю сайты, Telegram-ботов и автоматизацию бизнес-процессов. Помогаю экономить время и деньги — от MVP до полноценного продукта.</p>
      </div>

      <div class="card section">
        <strong>Услуги</strong>
        <div class="services" style="margin-top:10px;">
          <div class="service">
            <div style="font-weight:800">Сайты-визитки</div>
            <div style="color:var(--muted); margin-top:6px;">Лёгкая сборка, быстро и недорого.</div>
          </div>
          <div class="service">
            <div style="font-weight:800">Telegram-боты</div>
            <div style="color:var(--muted); margin-top:6px;">Боты для заявок, продаж и автоматизации.</div>
          </div>
          <div class="service">
            <div style="font-weight:800">Автоматизация</div>
            <div style="color:var(--muted); margin-top:6px;">Скрипты, интеграции, обработка данных.</div>
          </div>
        </div>
      </div>

      <div class="card section">
        <strong>Портфолио</strong>
        <div class="portfolio" style="margin-top:10px;">
          <div class="port-item">
            <img src="https://images.unsplash.com/photo-1515879218367-8466d910aaa4?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Проект 1">
            <div style="margin-top:8px; font-weight:700">Проект A</div>
            <div style="color:var(--muted); font-size:13px;">Сайт-визитка для бизнеса</div>
          </div>
          <div class="port-item">
            <img src="https://images.unsplash.com/photo-1522199710521-72d69614c702?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Проект 2">
            <div style="margin-top:8px; font-weight:700">Проект B</div>
            <div style="color:var(--muted); font-size:13px;">Telegram-бот для заявок</div>
          </div>
          <div class="port-item">
            <img src="https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Проект 3">
            <div style="margin-top:8px; font-weight:700">Проект C</div>
            <div style="color:var(--muted); font-size:13px;">Автоматизация процессов</div>
          </div>
        </div>
      </div>

      <div class="card section">
        <strong>Контакты</strong>
        <form id="contactForm" class="contact-form" onsubmit="return sendMail(event)">
          <div>
            <input class="input" id="inpName" placeholder="Имя" required>
            <input class="input" id="inpEmail" placeholder="Email" type="email" required style="margin-top:8px;">
            <textarea id="inpMsg" placeholder="Сообщение" required style="margin-top:8px;"></textarea>
          </div>
          <div style="display:flex; flex-direction:column; gap:8px;">
            <button class="btn" type="submit">Отправить</button>
            <a class="btn outline" id="callBtn" href="tel:+79990000000">Позвонить</a>
            <a class="btn outline" id="mailBtn" href="mailto:hello@example.com">Написать</a>
          </div>
        </form>
      </div>

      <div class="card section" style="text-align:center; padding:14px;">
        <small style="color:var(--muted)">Нужна кастомизация? Я могу быстро изменить цвета, фото, тексты и добавить домен — присылай изменения.</small>
      </div>
    </div>
  </div>

<script>
  // Personalize (edit values here or directly in HTML)
  const data = {
    name: 'Долгих Илья',
    role: 'Разработчик · Автоматизация · Сайты',
    phone: '+7 (999) 000-00-00',
    phoneHref: '+79990000000',
    email: 'hello@example.com',
    location: 'Красноярск, Россия',
    telegram: 'https://t.me/your_handle'
  };

  // populate
  document.getElementById('personName').textContent = data.name;
  document.getElementById('hiName').textContent = data.name.split(' ')[0] || data.name;
  document.getElementById('personRole').textContent = data.role;
  document.getElementById('phoneLink').textContent = data.phone;
  document.getElementById('phoneLink').href = 'tel:' + data.phoneHref;
  document.getElementById('callBtn').href = 'tel:' + data.phoneHref;
  document.getElementById('emailLink').textContent = data.email;
  document.getElementById('emailLink').href = 'mailto:' + data.email;
  document.getElementById('mailBtn').href = 'mailto:' + data.email;
  document.getElementById('locationText').textContent = data.location;
  document.getElementById('tgLink').href = data.telegram;
  document.getElementById('year').textContent = new Date().getFullYear();

  // vCard generation & download
  document.getElementById('downloadVcard').addEventListener('click', () => {
    const vcard =
`BEGIN:VCARD
VERSION:3.0
FN:${data.name}
ORG:${data.name}
TEL;TYPE=CELL:${data.phoneHref}
EMAIL;TYPE=INTERNET:${data.email}
ADR;TYPE=WORK:;;${data.location};;;
END:VCARD`;
    const blob = new Blob([vcard], {type: 'text/vcard'});
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = (data.name.replace(/\s+/g,'_') || 'contact') + '.vcf';
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
  });

  // theme toggle
  const themeToggle = document.getElementById('themeToggle');
  themeToggle.addEventListener('click', () => {
    if(document.body.classList.contains('light')){
      document.body.classList.remove('light');
      themeToggle.textContent = 'Светлая тема';
    } else {
      document.body.classList.add('light');
      themeToggle.textContent = 'Тёмная тема';
    }
  });

  // contact form: open mailto with prefilled body (simple fallback)
  function sendMail(e){
    e.preventDefault();
    const name = encodeURIComponent(document.getElementById('inpName').value.trim());
    const email = encodeURIComponent(document.getElementById('inpEmail').value.trim());
    const msg = encodeURIComponent(document.getElementById('inpMsg').value.trim());
    const subject = encodeURIComponent('Запрос с сайта от ' + (name || 'Посетителя'));
    const body = encodeURIComponent(`Имя: ${decodeURIComponent(name)}%0AEmail: ${decodeURIComponent(email)}%0A%0A${decodeURIComponent(msg)}`);
    const mailto = `mailto:${data.email}?subject=${subject}&body=${body}`;
    window.location.href = mailto;
    return false;
  }

  // Optional: quick drag & drop to change avatar image (for local editing)
  const avatar = document.getElementById('avatar');
  avatar.addEventListener('click', () => {
    const url = prompt('Вставьте URL изображения (или оставьте пустым):');
    if(url) {
      avatar.innerHTML = '<img src="'+url.replace(/"/g,'')+'" alt="Фото">';
    }
  });
</script>
</body>
</html>
